{
  "name": "chai-code-microservices",
  "version": "1.0.0",
  "description": "Root workspace for Chai Code microservices",
  "private": true,
  "scripts": {
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "test:auth": "npm --prefix services/auth-service run test:coverage",
    "test:course": "npm --prefix services/course-service run test:coverage",
    "sonar": "sonar-scanner -Dsonar.host.url=http://localhost:9000"
  },
  "devDependencies": {
    "sonarqube-scanner": "^4.2.6"
  }
}
